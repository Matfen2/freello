# Database (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=freello
DB_PASSWORD=freello
DB_DATABASE=freello

# App
PORT=3333

# JWT
JWT_ACCESS_SECRET=change-me-in-prod-very-random
JWT_ACCESS_EXPIRES_IN=15m

# Rate limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# CORS
CORS_ORIGIN=http://localhost:4200